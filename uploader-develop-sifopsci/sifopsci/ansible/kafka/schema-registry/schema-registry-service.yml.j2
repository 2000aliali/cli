apiVersion: v1
kind: Service
metadata:
  name: schema-registry-client-test
  labels:
    app: cp-registry
spec:
  ports:
  - name: schema-registry-client
    port: 8081
    targetPort: 8082
    protocol: TCP
  selector:
    name: cp-registry-test
